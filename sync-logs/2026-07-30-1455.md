# Nur AI daily sync — 2026-07-30 (14:55 UTC run)

Second sync of the day. The 05:40 run established the repository structure; this
run reconciled the installed agent skills against Notion and deepened the
generated configuration.

## Sources read

- Notion workspace: `Bilal Mohammed’s Space`
- Primary Nur AI definition: page `3ad8c1fe-1919-80e6-8895-ef90b55f3e17`
- Maryam role detail: page `3a78c1fe-1919-8074-a896-cd82eae74423`
- GitHub Pages go-live instructions: page `3ad8c1fe-1919-808e-9d59-ecfc0d5b82dc`
- Previous sync log page: `3ad8c1fe-1919-81e1-9225-fc7f2da56ae4`
- Installed skills: `nur-ai-maryam`, `nur-ai-aisha`, `nur-ai-ahmad`

## Reconciliation

No change to the locked product definition — Nur AI remains a subscription with
Personal ($30/month) and Business ($60/month), Aisha's brand outreach gated to
Business. Because the source-of-truth definition did not change, `index.html`
was left untouched, per rule 5 of the daily sync procedure in `SYNC.md`.

The installed agent skills carry operational detail that Notion does not: the
brand look, the camera baseline, Aisha's location deliverable, Ahmad's caption
formula and ten-point gate. That detail was folded into the generated config so
the repository is a complete representation of the running agents.

## Changes

`data/nur-ai.config.json` — schemaVersion 1 → 2

- Added `brand`: emerald-gold palette hexes, grade notes, typography, audio
  policy, copy policy, pacing.
- Added `qaGate`: Ahmad's ten checks, blocking, with failure routing.
- Added `cameraBaseline`: DJI Osmo Pocket 3 defaults, 180° shutter rule,
  exposure priority order, four lighting presets.
- Added `globalExclusions`: construction / BNZ Builders / estimating.
- Expanded each agent with voice, prefix, and lane-level deliverables — Maryam's
  Hormozi frameworks and seven-part output structure, Aisha's seven-part
  location deliverable and outreach principles, Ahmad's audio deliverable,
  caption formula, and post-timing basis.

`workflows/content-chain.json` — schemaVersion 1 → 2

- Added `trigger` phrase and fallback entry rule.
- Added per-step `outputs` and `exitCriteria`.
- Added `failureRouting`: owner-based routing, max 2 loops, escalation on
  exhaustion.
- Added two routing rules: Ahmad's scan is blocking; vocal-only audio.

`data/brand-profile.schema.json` — new

- JSON Schema for the persistent per-subscriber brand profile shared across the
  three agents: plan, voice, audience and timezone, kit, look, audio history
  (so Ahmad does not repeat tracks), outreach rate card, campaign tags.

## Conflicts

The older studio/agency framing and the newer locked subscription definition
still coexist in Notion. Precedence rule 1 applied — the locked definition won.
No pricing or public-offer decisions were made in this sync.

## Validation

- JSON parse of all three generated files: passed
- Agent set and handoff sequence (`maryam → aisha → ahmad → maryam`): passed
- No unresolved template placeholders: passed
- `git diff --check`: passed
- `scripts/validate-sync.ps1`: not run — no PowerShell in this environment

## Git result

Committed directly to `main` through the GitHub web editor, since this session
had no authenticated git remote. Four commits, one per file:

- `data/nur-ai.config.json` — schema v2
- `workflows/content-chain.json` — schema v2
- `data/brand-profile.schema.json` — new
- `sync-logs/2026-07-30-1455.md` — this log

Each file's contents were hash-verified (SHA-256) against the generated source
before committing. `index.html` was not modified, so the deployed site is
unchanged in appearance; GitHub Pages redeploys on each push.

Note: the commit message on the first commit was merged with a Copilot-suggested
draft and reads awkwardly. Content is unaffected.

## Next action

Wire `index.html` to read `data/nur-ai.config.json` at runtime so the public page
reflects config changes without a copy edit. Until then, agent-facing config and
public copy drift independently.
